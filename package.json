{
  "name": "potoboth-comit",
  "version": "1.0.0",
  "type": "module",
  "description": "COMIT Booth — Digital Photobooth",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build": "npm run build --prefix client",
    "vercel-build": "npm run install:all && npm run build --prefix client && cp -R client/dist public",
    "start": "node server/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "nodemailer": "^9.0.5"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
